# How to use it?

```bash
mkdir Code
cd Code
git clone https://github.com/wuyifan450/ren

mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE = Debug
cmake --build .
```

Windows下生成可执行文件
```bash
./bin/Release/NavCoreApp.exe
```

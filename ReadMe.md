# How to use it?

```bash
git clone <你的仓库地址>
cd NavCore

mkdir build
cd build
cmake .. -DCMAKE_BUILD_TYPE = Debug
cmake --build .
```

Windows下生成可执行文件
```bash
./bin/Release/NavCoreApp.exe
```
